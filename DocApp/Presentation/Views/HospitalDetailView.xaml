<Page
    x:Class="DocApp.Presentation.Views.HospitalDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DocApp.Presentation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:DocApp.Models"
    xmlns:uc="using:DocApp.Presentation.Views.Templates"
    mc:Ignorable="d"
    RequestedTheme="Dark"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    >
    <Page.Resources>
        <Style x:Key="SubTitleStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="10,10"/>
            <Setter Property="FontSize" Value="36"/>
            <Setter Property="FontFamily" Value="ms-appx:///Assets/raleway-v14-latin-900.ttf#Raleway"/>
        </Style>
        <Style x:Key="TextBlockStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="10,10"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>
    </Page.Resources>

    
        
        <SplitView x:Name="mySplitView" DisplayMode="Inline" IsPaneOpen="False" PanePlacement="Right" OpenPaneLength="400">
            <SplitView.Pane>
                <uc:DoctorProfileTemplate x:Name="DoctorProfile" DataContext="{x:Bind viewModel.doc}"/>
            </SplitView.Pane>
            <SplitView.Content>
            <ScrollViewer x:Name="myScroll">
                <StackPanel Margin="10,10,10,10" HorizontalAlignment="Stretch">

                    <StackPanel Orientation="Horizontal">
                        
                        
                        <Image Height="400" Width="400"/>
                        <StackPanel>

                            <TextBlock Text="{x:Bind viewModel.hospital.Name}" FontSize="24" FontWeight="Bold" Margin="10,10"/>
                            <TextBlock Text="{x:Bind viewModel.hospital.Location}" Style="{StaticResource TextBlockStyle}"/>
                            <TextBlock x:Name="HospitalRating" Style="{StaticResource TextBlockStyle}">
                    
                                <Run Text="{x:Bind viewModel.hospital.Rating.ToString()}"/>
                                <Run FontFamily="Segoe MDL2 Assets" Text="&#xE00A;"/>
                                <Run Text=" ("/>
                                <Run Text="{x:Bind viewModel.hospital.Number_Of_Rating.ToString()}"/>
                                <Run Text=" Ratings)"/> 
                            </TextBlock>
                            <TextBlock Text="{x:Bind viewModel.hospital.Description}" Style="{StaticResource TextBlockStyle}"/>

                        </StackPanel>

                    </StackPanel>




                    <TextBlock Text="Doctors In This Hospital" Style="{StaticResource SubTitleStyle}"/>

                    <ListView x:Name="myListView" ItemsSource="{x:Bind viewModel.Doctors}" Margin="10,10,10,10"
                              IsItemClickEnabled='True'
                          ItemClick="MyListView_ItemClick" HorizontalAlignment="Left">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <!--<Setter Property="Height" Value="200"/>-->
                                <Setter Property="Margin" Value="10,10,10,10"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="data:DoctorInHospitalDetails">
                                <uc:DoctorTemplate Margin="10,10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>


                </StackPanel>
            </ScrollViewer>
        </SplitView.Content>
            
        </SplitView>
        
        
    
</Page>
