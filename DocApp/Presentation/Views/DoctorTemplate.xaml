<UserControl
    x:Class="DocApp.Presentation.Views.DoctorTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DocApp.Presentation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <StackPanel Orientation="Horizontal">
            <Image Width="200" Height="200"/>
            <StackPanel>
                <StackPanel>
                    
                    <TextBlock x:Name="DoctorName" Text="{x:Bind Doctor.Name}"/>
                    <TextBlock x:Name="DoctorDesignation" Text="{x:Bind Doctor.Designation}"/>
                    <TextBlock x:Name="DoctorExperience" Text="{x:Bind Doctor.Experience}"/>

                </StackPanel>
                <StackPanel>
                    <TextBlock x:Name="DoctorFee" Text="{x:Bind Doctor.fees.ToString()}"/>
                    <TextBlock x:Name="DoctorStartTiming" Text="{x:Bind Doctor.Start_Time}"/>
                    <TextBlock x:Name="DoctorEndTiming" Text="{x:Bind Doctor.End_Time}"/>
                    <Button Content="Book Appointment" Margin="0,10,10,0"/>
                </StackPanel>
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>
